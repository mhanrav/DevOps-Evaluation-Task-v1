{
  "name": "jenkins-cicd-sample-app",
  "version": "1.0.0",
  "description": "Simple API for Jenkins CICD demo",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "jest-junit": "^13.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "jest-junit": {
    "outputDirectory": "test-results",
    "outputName": "junit.xml"
  }
}
